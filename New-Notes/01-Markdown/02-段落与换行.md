# Markdown段落与换行

**标签：** #Markdown #基础

---

## 1. 段落

连续两行文本之间用**空行**分隔：

```markdown
这是第一段（末尾无空格）

这是第二段（上面有空行）
```

---

## 2. 换行

**软换行**：在行末加**两个空格**然后回车：

```markdown
这是第一行（末尾有两个空格）  
这是第二行（紧接上一行软换行）
```

---

## 3. 注意事项

- 一个空格和两个空格的效果不同
- 部分渲染器支持 `\` 结尾换行，但不通用
- 空行分隔是Markdown的段落分隔标准方式
