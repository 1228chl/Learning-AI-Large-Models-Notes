---
author: "XunZong"
created: "2026-07-06"
tags: ["Python", "上下文管理器"]
aliases: ["上下文管理器", "with", "Context Manager"]
---

# 上下文管理器

## 定义

上下文管理器（Context Manager）是 Python 中用于**资源管理**的协议，最常见的场景是文件操作、数据库连接、锁获取等——需要"用完记得关闭"的操作。

使用 `with` 语句自动管理资源的**获取**和**释放**：

```python
# 传统写法：容易忘记 close
f = open("file.txt", "r")
data = f.read()
f.close()

# 上下文管理器：自动 close
with open("file.txt", "r") as f:
    data = f.read()
```

## 核心协议

上下文管理器通过两个魔术方法实现：

| 方法 | 调用时机 | 返回值 | 典型用途 |
|------|----------|--------|----------|
| `__enter__` | `with` 块开始时 | 赋值给 `as` 后的变量 | 获取资源 |
| `__exit__` | `with` 块结束时（无论是否异常） | `True`=阻止异常传播 | 释放资源 |

```python
class FileManager:
    def __init__(self, filename, mode):
        self.filename = filename
        self.mode = mode

    def __enter__(self):
        self.file = open(self.filename, self.mode)
        return self.file        # 赋值给 as 后的变量

    def __exit__(self, exc_type, exc_val, exc_tb):
        self.file.close()       # 无论如何都会执行
        return False            # False = 让异常继续传播
```

## `contextlib` 模块

使用标准库 `contextlib` 可以更简洁地创建上下文管理器：

| 工具 | 用途 | 示例 |
|------|------|------|
| `@contextmanager` | 用生成器语法定义 | `yield` 前是 `__enter__`，后是 `__exit__` |
| `closing()` | 调用对象的 `close()` 方法 | `closing(urllib.urlopen(...))` |
| `suppress()` | 忽略指定异常 | `suppress(FileNotFoundError)` |
| `redirect_stdout` | 临时重定向输出 | 捕获 `print` 输出 |

```python
from contextlib import contextmanager

@contextmanager
def managed_file(filename, mode):
    f = open(filename, mode)
    try:
        yield f                 # __enter__ 返回的值
    finally:
        f.close()              # __exit__ 中执行
```

## ML 中的典型应用

| 场景 | 代码模式 | 作用 |
|------|----------|------|
| **PyTorch 推理** | `with torch.no_grad():` | 关闭梯度计算，节省显存和计算 |
| **模型训练** | `with torch.autocast(device):` | 自动混合精度训练 |
| **日志记录** | `with SummaryWriter() as writer:` | 自动 flush 和关闭写入器 |
| **分布式训练** | `with model.no_sync():` | 临时关闭梯度同步 |
| **梯度累积** | `with torch.no_grad():` | 评估模式禁用梯度跟踪 |

> 参见 [[03-装饰器]]、[[04-迭代器与生成器]]、[[01-类与对象]]
