---
author: "XunZong"
created: "2026-07-06"
tags: ["数据结构", "树", "二叉树"]
aliases: ["树", "二叉树", "BST", "遍历"]
---

# 树与二叉树

## 定义

树（Tree）是一种**非线性**数据结构，由节点（Node）和边（Edge）组成。每个节点有零个或多个**子节点**，有且只有一个**根节点**。二叉树（Binary Tree）是每个节点最多有两个子节点（左孩子、右孩子）的树。

## 二叉树遍历

```python
class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right

# 深度优先遍历（DFS）— 用递归或栈
def inorder(root):           # 中序：左→根→右
    if not root: return
    inorder(root.left)
    print(root.val)
    inorder(root.right)

def preorder(root):          # 前序：根→左→右
    if not root: return
    print(root.val)
    preorder(root.left)
    preorder(root.right)

def postorder(root):         # 后序：左→右→根
    if not root: return
    postorder(root.left)
    postorder(root.right)
    print(root.val)

# 广度优先遍历（BFS）— 用队列
from collections import deque
def level_order(root):
    if not root: return
    q = deque([root])
    while q:
        node = q.popleft()
        print(node.val)
        if node.left: q.append(node.left)
        if node.right: q.append(node.right)
```

## 二叉搜索树（BST）

对任意节点，**左子树所有值 < 节点值 < 右子树所有值**：

| 操作 | 平均 | 最坏（退化为链表） |
|------|:----:|:------------------:|
| 查找 | $O(\log n)$ | $O(n)$ |
| 插入 | $O(\log n)$ | $O(n)$ |
| 删除 | $O(\log n)$ | $O(n)$ |

BST 的不平衡问题由平衡树解决：**AVL 树**（严格平衡）和**红黑树**（近似平衡，Java TreeMap、C++ map 的底层）。

## 二叉树分类

| 类型 | 定义 | 用途 |
|------|------|------|
| **满二叉树** | 所有层节点数达到最大 | 理论分析 |
| **完全二叉树** | 除最后一层外都满，且最后一层靠左 | **堆**的底层存储 |
| **二叉搜索树（BST）** | 左小右大 | 有序数据存储 |
| **平衡二叉树（AVL）** | 任意节点左右子树高度差 ≤ 1 | 严格平衡的 BST |
| **红黑树** | 近似平衡，旋转少 | 工程最常用（Map底层） |
| **线段树** | 区间查询和更新 | 区间最值、区间和 |
| **Trie（前缀树）** | 字符串前缀匹配 | 自动补全、词典 |

## ML 中的树

| 场景 | 使用方式 |
|------|----------|
| **决策树 / 随机森林** | 二叉树结构做分类/回归 |
| **XGBoost / LightGBM** | 梯度提升树（GBDT） |
| **Transformer 位置编码** | 树的层次结构编码位置关系 |
| **语法解析树** | NLP 中的句法分析 |
| **KD-Tree / Ball Tree** | KNN 加速搜索 |

> 参见 [[08-堆]]、[[09-图]]、[[12-递归与分治]]
