---
author: "XunZong"
created: "2026-07-06"
tags: ["数据结构", "堆", "优先队列"]
aliases: ["堆", "优先队列", "Heap", "Priority Queue"]
---

# 堆

## 定义

堆（Heap）是一种**完全二叉树**结构，且任意父节点的值总是大于等于（**最大堆**）或小于等于（**最小堆**）其子节点的值。

**底层存储**：用数组实现完全二叉树——节点 $i$ 的左子节点索引为 $2i+1$，右子节点为 $2i+2$，父节点为 $\lfloor (i-1)/2 \rfloor$。

## 核心操作复杂度

| 操作 | 时间复杂度 | 说明 |
|------|:----------:|------|
| `push(x)` | $O(\log n)$ | 插入到末尾，上浮（sift up） |
| `pop()` | $O(\log n)$ | 弹出堆顶，末尾元素放到堆顶，下沉（sift down） |
| `peek()` | $O(1)$ | 查看堆顶（根节点） |
| `build()` | $O(n)$ | 从无序数组建堆（Floyd 算法） |
| `heapify` | $O(\log n)$ | 单个节点下沉调整 |

```python
import heapq

# Python 默认是最小堆
heap = []
heapq.heappush(heap, 5)       # push
heapq.heappush(heap, 1)
heapq.heappush(heap, 3)
smallest = heapq.heappop(heap)  # pop → 1

# 最大堆：取负数
max_heap = []
heapq.heappush(max_heap, -5)    # 存负值
heapq.heappush(max_heap, -1)
largest = -heapq.heappop(max_heap)  # → 5

# 建堆 O(n)
nums = [3, 1, 4, 1, 5, 9]
heapq.heapify(nums)             # 原地建堆

# Top-K 问题
top_k = heapq.nlargest(3, nums)   # [9, 5, 4]
```

## 堆排序

```python
def heap_sort(nums):
    heapq.heapify(nums)           # 建最小堆
    return [heapq.heappop(nums) for _ in range(len(nums))]
```

堆排序时间复杂度 $O(n\log n)$，空间复杂度 $O(1)$（原地排序），但不稳定。

## 堆 vs 二叉搜索树

| 对比 | 堆 | BST |
|------|:--:|:---:|
| 结构约束 | 父 ≥ 子（或 ≤） | 左 < 根 < 右 |
| 查找 | $O(n)$（略无序） | $O(\log n)$ |
| 取最值 | $O(1)$ 堆顶 | $O(\log n)$ 最左/右 |
| 插入 | $O(\log n)$ | $O(\log n)$ |
| 主用途 | **优先队列** | **有序数据** |

## 应用场景

| 场景 | 原理 | 示例 |
|------|------|------|
| **优先队列** | 按优先级出队 | 任务调度、Dijkstra 最短路径 |
| **Top-K 问题** | 固定大小堆 | 前 K 大/小元素 |
| **中位数查找** | 最大堆 + 最小堆 | 数据流中位数 |
| **合并 K 个有序序列** | 最小堆存每个序列的头 | 外部排序 |
| **定时任务** | 按时间排序的最小堆 | 定时器调度 |

> 参见 [[07-树与二叉树]]、[[10-排序算法]]、[[11-搜索算法]]
