---
author: "XunZong"
created: "2026-07-06"
tags: ["机器学习", "Boosting", "XGBoost"]
aliases: ["GBDT", "XGBoost", "LightGBM", "梯度提升", "Gradient Boosting"]
---

# 梯度提升机（GBDT / XGBoost / LightGBM）

## 定义

梯度提升（Gradient Boosting）是 Boosting 流派中最成功的方法。它**串行**训练决策树，每棵新树学习**前一棵树的残差**（实际值 - 预测值），逐步降低整体偏差。

```python
# GBDT（sklearn 原生）
from sklearn.ensemble import GradientBoostingClassifier

gbdt = GradientBoostingClassifier(
    n_estimators=100,
    max_depth=3,
    learning_rate=0.1               # 每棵树的贡献幅度
)
gbdt.fit(X_train, y_train)
```

## 核心思想

$$
\text{第 t 步:} \quad F_t(x) = F_{t-1}(x) + \eta \cdot h_t(x)
$$

其中 $h_t(x)$ 是拟合**负梯度**（残差方向）的新树， $\eta$ 是学习率。

```python
# 手动理解 GBDT 的串行逻辑
from sklearn.tree import DecisionTreeRegressor

# 第一棵树：直接拟合原始目标
tree1 = DecisionTreeRegressor(max_depth=3)
tree1.fit(X, y)
residual1 = y - tree1.predict(X)    # 残差

# 第二棵树：拟合残差
tree2 = DecisionTreeRegressor(max_depth=3)
tree2.fit(X, residual1)
residual2 = residual1 - tree2.predict(X)  # 更新残差

# 集成预测 = tree1 + lr * tree2 + lr * tree3 + ...
```

## XGBoost — 工业级实现

XGBoost 在原始 GBDT 基础上做了大量优化：

```python
import xgboost as xgb

model = xgb.XGBClassifier(
    n_estimators=200,
    max_depth=6,
    learning_rate=0.1,
    subsample=0.8,                  # 行采样（防止过拟合）
    colsample_bytree=0.8,           # 列采样（类似随机森林）
    reg_alpha=0.1,                  # L1 正则化
    reg_lambda=1.0,                 # L2 正则化
    eval_metric='logloss',
    early_stopping_rounds=20,        # 早停
    use_label_encoder=False
)

model.fit(
    X_train, y_train,
    eval_set=[(X_val, y_val)],
    verbose=False
)
```

## LightGBM — 更快更强

```python
import lightgbm as lgb

lgb_model = lgb.LGBMClassifier(
    n_estimators=200,
    num_leaves=31,                  # 叶子数（控制复杂度）
    max_depth=-1,                   # 不限深度
    learning_rate=0.1,
    subsample=0.8,
    colsample_bytree=0.8,
    reg_alpha=0.1,
    reg_lambda=1.0
)

lgb_model.fit(
    X_train, y_train,
    eval_set=[(X_val, y_val)],
    callbacks=[lgb.early_stopping(20)]
)
```

## 三种主流实现对比

| 特性 | GBDT（sklearn） | XGBoost | LightGBM |
|------|:--------------:|:-------:|:--------:|
| **分裂策略** | 逐层分裂（Level-wise） | 逐层分裂 | 逐叶分裂（Leaf-wise） |
| **训练速度** | 慢 | 中 | **最快** |
| **内存占用** | 高 | 中 | **最低** |
| **分类特征** | ❌ 需编码 | ❌ 需编码 | ✅ **原生支持** |
| **缺失值处理** | ❌ 需填充 | ✅ 自动学习分裂方向 | ✅ 自动处理 |
| **GPU 加速** | ❌ | ✅ | ✅ |
| **分布式** | ❌ | ✅ | ✅ |
| **精度** | 基准 | 略高 | 与 XGBoost 相当 |

## 超参数速查

| 参数 | 控制 | 调高 | 调低 |
|------|------|:----:|:----:|
| `n_estimators` | 树的数量 | 过拟合 | 欠拟合 |
| `learning_rate` | 每棵树贡献 | 过拟合 | 欠拟合 |
| `max_depth` / `num_leaves` | 树的复杂度 | 过拟合 | 欠拟合 |
| `subsample` | 行采样比例 | 过拟合 | 欠拟合 |
| `reg_alpha` / `reg_lambda` | 正则化强度 | 欠拟合 | 过拟合 |

> **Kaggle 结构化数据比赛的霸主**。绝大多数表格数据竞赛冠军方案都基于 XGBoost / LightGBM。

## 面试追问

**Q1（基础）**：梯度提升（Gradient Boosting）的核心思想是什么？每棵新树学的是什么？

**回答要点**：串行训练决策树，每棵新树拟合前一棵树的残差（实际值 - 预测值）的负梯度方向；最终预测是所有树的加权求和 Fₜ(x)=Fₜ₋₁(x)+η·hₜ(x)；通过逐步降低偏差来提升整体性能，每棵树只贡献一小部分（由学习率 η 控制）。

**Q2（深挖）**：XGBoost 相比传统 GBDT 做了哪些关键优化？

**回答要点**：XGBoost 加入了正则化项（L1+L2）防止过拟合；支持列采样（类似随机森林）；对损失函数做二阶泰勒展开使用梯度和 Hessian 矩阵，收敛更快；自动处理缺失值学习分裂方向；支持并行化（特征维度并行）；有早停机制防止过拟合。

**Q3（实战）**：LightGBM 的"逐叶分裂"（Leaf-wise）相比 XGBoost 的"逐层分裂"（Level-wise）有什么优势和风险？

**回答要点**：Leaf-wise 每次只分裂增益最大的叶子，能更快降低损失，训练速度更快、内存占用更低；但容易长出深度不平衡的树，更容易过拟合；需通过 num_leaves 和 max_depth 限制叶子数和深度来防止过拟合；Level-wise 更保守但更稳定。

**Q4（边界）**：梯度提升模型对异常值敏感吗？在什么数据结构下表现不佳？

**回答要点**：GBDT/Boosting 对异常值比随机森林更敏感，因为每棵树拟合残差——异常值会产生极大残差，后续树拼命拟合这些噪声导致过拟合；对高维稀疏数据（如文本）不如线性模型 + L1 正则化；分类特征多时 LightGBM 原生支持更好但 XGBoost 需手动编码。

## 参考引用
- 需要理解集成学习的相关知识，参见 [集成学习](./12-集成学习.md)
- 需要理解随机森林的相关知识，参见 [随机森林](./13-随机森林.md)
- 需要理解决策树的相关知识，参见 [决策树](./11-决策树.md)