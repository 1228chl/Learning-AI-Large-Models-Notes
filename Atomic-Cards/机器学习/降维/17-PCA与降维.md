---
author: "XunZong"
created: "2026-07-06"
tags: ["机器学习", "降维", "PCA"]
aliases: ["PCA", "主成分分析", "降维", "t-SNE"]
---

# PCA 与降维

## 定义

主成分分析（Principal Component Analysis, PCA）是最常用的**无监督线性降维**方法。它找到数据中**方差最大的方向**（主成分），将数据投影到低维子空间，同时尽可能保留原始数据的方差信息。

## 直观理解

### PCA 的主轴方向解释

将 $n$ 维数据想象成一个在空间中散布的"数据云"。PCA 的几何本质是：**找到这个数据云被拉伸得最长的那些方向**。

- **第一主成分**：数据云最"长"的方向（方差最大的方向）
- **第二主成分**：与第一主成分正交的方向中，数据最"长"的方向
- 以此类推，直到第 $k$ 个主成分——这些方向在数据云中构成了一组正交的"主轴"

协方差矩阵 $\Sigma$ 的特征向量就是这些主轴方向，特征值就是各主轴上的"长度"（方差大小）。丢弃小特征值方向相当于忽略数据云中的"薄片"——这些方向上的变化很小，主要来自噪声而非信号。

### 为什么 PCA 需要标准化？

PCA 寻找"方差最大的方向"。如果特征 $A$ 的单位是米（范围 0-100），特征 $B$ 的单位是毫米（范围 0-100000），$B$ 的方差天然比 $A$ 大 $10^6$ 倍，PCA 会优先选择 $B$ 的方向——但这只是量纲差异，而非真正的数据结构。对特征做标准化（均值为 0、方差为 1）后，所有特征在同一尺度上贡献，PCA 才能找到真正的"结构方向"。

### PCA 降维的投影视角

想象一个三维空间中的椭圆体（数据云）。从某些角度看去，它几乎是一个平面（二维），甚至是一条线（一维）。PCA 相当于找到"最能看到数据全貌"的观察角度——即方差最大的投影方向。丢弃次要维度相当于从信息损失最小的角度观察数据。

```python
from sklearn.decomposition import PCA
import matplotlib.pyplot as plt

# 降维到 2 维（用于可视化），将高维数据投影到方差最大的两个方向上
pca = PCA(n_components=2)

X_2d = pca.fit_transform(X)  # 拟合PCA模型并将数据投影到2维空间

print(f"解释方差比: {pca.explained_variance_ratio_}")  # 每个主成分解释的方差比例
print(f"累积方差: {pca.explained_variance_ratio_.cumsum()}")  # 前k个主成分累积解释的方差比例
# 可视化：用2维散点图展示高维数据的聚类结构
plt.scatter(X_2d[:, 0], X_2d[:, 1], c=y, cmap='viridis')
plt.xlabel('PC1'); plt.ylabel('PC2')
```

## 数学本质

设 $X$ 为数据矩阵，$n$ 为样本数。PCA 对**协方差矩阵** $\Sigma = \frac{1}{n} X^T X$ 做**特征值分解**：

$$
\Sigma \mathbf{v}_i = \lambda_i \mathbf{v}_i
$$

- 特征向量 $\mathbf{v}_i$ → 主成分方向
- 特征值 $\lambda_i$ → 该方向上的方差大小

```python
# 选择主成分数量：自动保留解释95%方差所需的最少主成分数
pca = PCA(n_components=0.95)          # 保留 95% 的方差，丢弃5%的噪声/冗余

X_reduced = pca.fit_transform(X)
print(f"保留 {pca.n_components_} 个主成分")

# 查看各特征的贡献（载荷矩阵）：每个主成分是原始特征的加权线性组合
loadings = pca.components_            # 每一行是一个主成分，值为原始特征的权重
```

## PCA 的应用

| 用途 | 说明 |
|------|------|
| **数据可视化** | 降到 2D/3D 后 scatter plot |
| **去噪** | 丢弃方差小（即噪声）的维度 |
| **加速训练** | 减少特征数，加速模型训练 |
| **缓解维度灾难** | 高维 → 低维，距离计算更稳定 |
| **多重共线性处理** | PCA 去相关后再建模 |

```python
# 用 PCA 加速分类：先降维再训练分类器，减少特征数降低模型复杂度
pipe = Pipeline([

    ('pca', PCA(n_components=100)),          # 先降到100维，去除噪声和冗余特征
    ('clf', RandomForestClassifier())        # 在降维后的特征上训练随机森林
])
pipe.fit(X_train, y_train)  # 流水线自动完成先降维再分类的流程
```

## 线性 vs 非线性降维

降维方法分为线性（PCA）和非线性（t-SNE、UMAP）两大类，各自适用不同场景：

| 降维方法 | 线性？ | 核心思想 | 适用场景 | 详细卡片 |
|:--------:|:------:|:---------|:---------|:--------:|
| **PCA** | ✅ | 保留最大方差方向（协方差矩阵特征分解） | 加速训练、去噪、特征提取 | [PCA主成分分析](29-PCA主成分分析.md) |
| **t-SNE** | ❌ | 保留局部邻域（概率分布匹配） | 数据可视化探索 | [t-SNE与UMAP非线性降维](30-t-SNE与UMAP非线性降维.md) |
| **UMAP** | ❌ | 流形学习 + 拓扑数据分析 | 可视化、可作特征 | [t-SNE与UMAP非线性降维](30-t-SNE与UMAP非线性降维.md) |

## 何时使用

- **用 PCA**：需要加速训练、去噪、去相关、特征数 > 样本数、需要可逆变换
- **用 t-SNE/UMAP**：仅用于可视化探索、数据有复杂流形结构

## 面试追问

**Q1（基础）**：PCA 和 t-SNE 的核心区别是什么？分别在什么场景下使用？
**回答要点**：

1. PCA 是线性降维方法，通过最大化方差方向保留全局数据结构；t-SNE 是非线性降维，通过概率分布匹配保留局部邻域关系
2. PCA 适合加速训练、去噪、去相关和特征提取；t-SNE 仅适用于数据可视化探索
3. PCA 可逆且结果确定，t-SNE 不可逆且每次运行结果不同

**Q2（深挖）**：PCA 中"解释方差比"（explained variance ratio）的含义是什么？如何根据它选择主成分数量？
**回答要点**：

1. 解释方差比 = 该主成分对应特征值 / 所有特征值之和，表示该方向保留的原始方差比例
2. 累积方差比达到阈值法：选择使累积方差比达到 95%（或其他阈值）的最少主成分数
3. 特征值大于 1 准则（Kaiser 准则）或 Scree Plot 找"肘部"拐点

**Q3（实战）**：在使用 PCA 处理真实数据时，是否需要对特征进行标准化/归一化？为什么？
**回答要点**：

1. 必须标准化。PCA 寻找方差最大的方向，不同量纲的特征会导致方差被量纲大的特征主导
2. 若一个特征范围 0-1，另一个 0-10000，后者方差远大于前者，主成分方向会被扭曲
3. 正确做法：先做 StandardScaler（标准化为均值为 0、方差为 1）再进行 PCA

**Q4（边界）**：PCA 的线性假设在什么情况下会失效？如何判断数据是否需要非线性降维？
**回答要点**：

1. 数据位于非线性流形上（如瑞士卷、圆环分布）时，PCA 无法有效展开低维结构
2. 判断方法：PCA 需要很多维度才能达到累积方差阈值，或降维后类别在低维空间中混叠严重
3. 此时应使用 t-SNE、UMAP 或自编码器等非线性降维方法

## 参考引用
- 需要理解PCA主成分分析的相关知识，参见 [PCA主成分分析](29-PCA主成分分析.md)
- 需要理解t-SNE与UMAP非线性降维的相关知识，参见 [t-SNE与UMAP非线性降维](30-t-SNE与UMAP非线性降维.md)
- 需要掌握特征值分解以理解向量运算与相似度计算的数学基础，参见 [特征值分解](../../线性代数/特征分解/10-特征值分解.md)
