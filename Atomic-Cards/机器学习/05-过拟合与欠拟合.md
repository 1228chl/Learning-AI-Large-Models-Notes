---
author: "XunZong"
created: "2026-07-06"
tags: ["机器学习", "过拟合", "欠拟合"]
aliases: ["过拟合", "欠拟合", "偏差-方差", "Overfitting", "Underfitting"]
---

# 过拟合与欠拟合

## 定义

| 问题 | 表现 | 训练误差 | 测试误差 | 原因 |
|------|------|:--------:|:--------:|------|
| **欠拟合（Underfitting）** | 模型太简单，学不到规律 | 高 | 高 | 模型容量不足 |
| **过拟合（Overfitting）** | 模型太复杂，记忆了噪声 | 低 | 高 | 模型容量过大 |

## 诊断：学习曲线

```python
from sklearn.model_selection import learning_curve
import matplotlib.pyplot as plt

train_sizes, train_scores, val_scores = learning_curve(
    model, X, y, cv=5, train_sizes=np.linspace(0.1, 1.0, 10)
)

# 过拟合：训练得分高（>0.95），验证得分低（<0.8），且差距大
# 欠拟合：两者都低（<0.7），且趋近于平行
# 拟合良好：两者都高且接近
```

## 偏差-方差权衡（Bias-Variance Tradeoff）

$$\text{泛化误差} = \text{偏差}^2 + \text{方差} + \text{噪声}$$

| 分量 | 含义 | 欠拟合 | 过拟合 |
|------|------|:------:|:------:|
| **偏差（Bias）** | 模型预测的期望与真实值的差距 | 高 | 低 |
| **方差（Variance）** | 不同训练集上预测的波动程度 | 低 | 高 |
| **噪声（Noise）** | 数据本身的不可约误差（下限） | 不变 | 不变 |

## 应对策略

```python
# 模型复杂度控制示例：多项式回归的 degree 选择
from sklearn.pipeline import Pipeline
from sklearn.preprocessing import PolynomialFeatures

for degree in [1, 3, 10]:
    model = Pipeline([
        ('poly', PolynomialFeatures(degree=degree)),
        ('linear', LinearRegression())
    ])
    model.fit(X_train, y_train)
    train_score = model.score(X_train, y_train)
    test_score = model.score(X_test, y_test)
    # degree=1: 欠拟合（两低）, degree=10: 过拟合（训练高测试低）
    # degree=3: 最佳折中
```

| 策略 | 解决过拟合 | 解决欠拟合 |
|------|:---------:|:---------:|
| **增加训练数据** | ✅ 最强 | ❌ |
| **降低模型复杂度**（减少层/树深） | ✅ | ❌ |
| **正则化（L1/L2）** | ✅ | ❌ |
| **早停（Early Stopping）** | ✅ | ❌ |
| **Dropout** | ✅ | ❌ |
| **数据增强** | ✅ | ❌ |
| **增加模型复杂度** | ❌ | ✅ |
| **增加特征** | ❌ | ✅ |
| **减少正则化** | ❌ | ✅ |
| **训练更长时间** | ❌ | ✅ |

```python
# 正则化解过拟合
from sklearn.linear_model import Ridge, Lasso

ridge = Ridge(alpha=1.0)    # L2 正则化
lasso = Lasso(alpha=0.01)   # L1 正则化（产生稀疏解）

# 早停
from sklearn.model_selection import validation_curve
```

> 参见 [[03-数据集划分与交叉验证]]、[[20-正则化]]、[[04-评估指标]]
