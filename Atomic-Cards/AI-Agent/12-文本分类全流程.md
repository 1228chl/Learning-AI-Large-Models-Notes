---
author: "XunZong"
created: "2026-07-06"
tags: ["AI-Agent", "项目实战", "分类"]
aliases: ["文本分类", "NLP项目流程"]
---

# 文本分类全流程

## 定义

文本分类是 NLP 中最基础也最实用的任务。一个完整的文本分类项目包含从**数据获取到模型部署**的完整流水线。

## 项目全流程

```python
数据获取 → 数据探索(EDA) → 预处理 → 特征工程 → 模型训练 → 评估 → 部署
```

## 1. 数据获取与探索

```python
import pandas as pd
import matplotlib.pyplot as plt

# 从CSV文件读取新闻数据，包含文本内容和类别标签
df = pd.read_csv('news_data.csv')
# 标签分布：统计各类别样本数量并绘制柱状图，用于检查类别是否平衡
df['category'].value_counts().plot(kind='bar')
# 文本长度分布：计算每条文本的字符长度，为设置截断长度提供参考
df['text_len'] = df['text'].apply(len)
# 绘制长度直方图，观察数据长度分布情况，确定合适的max_length参数
df['text_len'].hist(bins=50)

# 常见问题：
# - 类别不平衡？→ 重采样 / 加权损失
# - 样本量？→ 小样本考虑预训练 + 微调
# - 数据质量？→ 去重、去噪声、统一编码
```

## 2. 预处理与特征工程

```python
import jieba
from sklearn.feature_extraction.text import TfidfVectorizer

# 中文预处理
# 定义文本预处理函数：清洗文本并进行中文分词
def preprocess(text):
    # 将换行符替换为空格并去除首尾空白，统一文本格式
    text = text.replace('\n', ' ').strip()
    words = jieba.lcut(text)            # 使用结巴分词将文本切分为词语列表
    words = [w for w in words if len(w) > 1]  # 过滤单字词，保留有实际语义的多字词
    return ' '.join(words)              # 将分词结果用空格拼接，适配sklearn输入格式

# 传统 ML 特征
# 创建TF-IDF向量化器：保留最多5000个特征，同时提取单个词和相邻双词组合特征
vectorizer = TfidfVectorizer(max_features=5000, ngram_range=(1, 2))
# 对语料库进行拟合并转换为TF-IDF特征矩阵，供传统机器学习模型使用
X_tfidf = vectorizer.fit_transform(corpus)

# 深度学习：直接用 tokenizer
from transformers import AutoTokenizer
# 加载BERT中文预训练模型的tokenizer，用于将文本转换为模型可接受的输入格式
tokenizer = AutoTokenizer.from_pretrained('bert-base-chinese')
# 对文本进行批处理编码：自动填充至等长、截断超长文本，最大长度设为128个token
inputs = tokenizer(texts, padding=True, truncation=True, max_length=128)
```

## 3. 模型选择

```python
from sklearn.linear_model import LogisticRegression
from sklearn.ensemble import RandomForestClassifier
import lightgbm as lgb

# 基线：传统 ML（Tfidf + 分类器）
# 创建逻辑回归基线模型，设置最大迭代次数确保收敛，适合作为分类任务的首个基准
baseline = LogisticRegression(max_iter=1000)

# 中档：FastText
import fasttext
# 训练FastText文本分类模型，使用25轮迭代并提取双词n-gram特征以增强上下文理解
model = fasttext.train_supervised('train.txt', epoch=25, wordNgrams=2)

# 高阶：BERT 微调
from transformers import AutoModelForSequenceClassification
# 加载BERT中文预训练模型，并添加序列分类头，准备在下游分类任务上进行微调
model = AutoModelForSequenceClassification.from_pretrained('bert-base-chinese')
```

```python
# 模型选择策略
# < 1000 样本: 传统 ML（TF-IDF + LR/RF）
# 1000 - 10000 样本: FastText / 传统 ML
# > 10000 样本: BERT 微调 / 预训练 + 微调
```

## 4. 训练与评估

```python
from sklearn.metrics import classification_report

# 评估
# 使用训练好的模型对测试集进行预测
y_pred = model.predict(X_test)
# 输出分类评估报告：包含每类精确率、召回率、F1分数及整体准确率
print(classification_report(y_test, y_pred, target_names=labels))

# 混淆矩阵
from sklearn.metrics import ConfusionMatrixDisplay
# 绘制混淆矩阵热力图，直观展示模型在每个类别上的正确与错误预测分布
ConfusionMatrixDisplay.from_estimator(model, X_test, y_test)
```

## 5. 模型对比案例（图书分类）

| 方案 | 模型 | 精度 | 训练时间 | 推理速度 |
|:----:|:----|:----:|:--------:|:--------:|
| **方案 A** | 随机森林 + TF-IDF | 61% | 快 | 极快 |
| **方案 B** | FastText (n-gram=2) | **94%** | 快 | 极快 |
| **方案 C** | BERT-base 微调 | 93% | 慢 | 慢 |

> **FastText 胜出原因**：81 万数据 + 40 类 + 子词特征的优势

## ML 中的文本分类

| 应用场景 | 数据量 | 推荐方案 | 期望精度 |
|:--------:|:-----:|:--------|:--------:|
| **垃圾邮件检测** | 万级 | FastText / BERT | 95%+ |
| **新闻分类** | 万-百万级 | BERT / FastText | 90%+ |
| **情感分析** | 千-万级 | 传统 ML / BERT | 85%+ |
| **意图识别** | 万级 | BERT | 95%+ |

## 面试追问

**Q1（基础）**：一个完整的文本分类项目包含哪些步骤？如何根据数据量选择不同的模型方案？

**回答要点**：数据获取→EDA→预处理→特征工程→模型训练→评估→部署；<1000 样本推荐传统 ML（TF-IDF + LR/RF），1000-10000 推荐 FastText，>10000 推荐 BERT 微调；小样本不适合深度学习方法——容易过拟合，DL 需要大数据量才能发挥预训练优势。

**Q2（深挖）**：TF-IDF + 逻辑回归和 BERT 微调两种方案在特征表示和分类原理上有什么本质区别？

**回答要点**：TF-IDF 基于词袋模型，将文本转为稀疏向量（维度=词表大小），LR 做线性决策边界——可解释性强、训练快但无法捕捉词序和上下文语义；BERT 基于 Transformer 预训练，通过注意力机制学习上下文相关的稠密向量表示——语义理解强但需要 GPU 资源、推理慢。

**Q3（实战）**：案例中 FastText 在 81 万数据 40 类任务上以 94% 精度超越 BERT 的 93%，分析了哪些原因？这对实际项目选型有什么启示？

**回答要点**：FastText 利用子词 n-gram 特征捕捉形态学信息，对中文新词和变体鲁棒；81 万数据量对 FastText 已经足够饱和但 BERT 微调未必充分收敛；FastText 采用 hierarchical softmax 在类别多时训练更快；启示：重视基准模型（简单方法在充分调优后可能接近或超越复杂模型），选型需综合考量数据量、类别数、延迟要求而非一味追求"最先进"。

**Q4（边界）**：文本分类系统上线后可能遇到哪些训练阶段未见的挑战？如何应对？

**回答要点**：数据分布漂移——线上数据分布与训练集不一致导致准确率下降，需监控置信度分布并触发重训；新类别出现——需设计 OOD 检测机制（如基于预测熵或距离度量），并规划增量学习流程；长文本截断丢失关键分类特征——需根据业务定制 truncation 策略或使用长文本模型；推理延迟和吞吐量要求——需模型量化/剪枝/蒸馏，或用传统 ML 做快速预过滤再深度模型兜底。

## 参考引用
- 需要理解FastText与子词信息的自然语言处理原理，参见 [FastText与子词信息](../NLP/03-FastText与子词信息.md)
- 需要理解BERT与MLM预训练的自然语言处理原理，参见 [BERT与MLM预训练](../NLP/10-BERT与MLM预训练.md)
- 需要理解模型压缩量化剪枝蒸馏的深度学习机制与实现，参见 [模型压缩量化剪枝蒸馏](../深度学习/15-模型压缩量化剪枝蒸馏.md)