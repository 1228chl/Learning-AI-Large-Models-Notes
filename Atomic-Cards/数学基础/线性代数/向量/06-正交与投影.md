---
author: "XunZong"
created: "2026-07-06"
tags: ["数学", "线性代数", "正交", "投影"]
aliases: ["正交", "投影", "正交投影", "Gram-Schmidt"]
---

# 正交与投影

## 正交性

两个向量 $\mathbf{u}, \mathbf{v}$ 正交（Orthogonal / Perpendicular）当且仅当 $\mathbf{u} \cdot \mathbf{v} = 0$ 。正交向量之间的夹角为 $90^\circ$ ，彼此不含对方的信息成分。

**正交矩阵** $Q$ 满足 $Q^T Q = I$ ，其列向量构成一组标准正交基。正交变换保长度（ $\|Q\mathbf{x}\| = \|\mathbf{x}\|$ ），在数值计算中极其稳定。

## 几何直觉

### 正交的几何含义

两个向量正交的本质是：**一个向量在另一个向量方向上的投影为零**。用点积衡量： $\mathbf{u} \cdot \mathbf{v} = \|\mathbf{u}\|\|\mathbf{v}\|\cos 90^\circ = 0$ 。在 ML 中，正交性对应"信息不重叠"：

- **特征解耦**：正交的特征向量捕获数据中互不相关的变化方向（PCA 的主成分正是协方差矩阵的特征向量，它们彼此正交）
- **正交初始化**：权重矩阵 $W^T W = I$ 保证每层输入信号被投射到互不干扰的方向上，梯度不会沿某一方向过度积累

### 投影的几何解释

向量 $\mathbf{v}$ 到子空间 $S$ 的正交投影 $\mathbf{v}_\parallel$ 是 $S$ 中与 $\mathbf{v}$ 距离最近的点。几何含义：

- 将 $\mathbf{v}$ 分解为 $\mathbf{v} = \mathbf{v}_\parallel + \mathbf{v}_\perp$ ，其中 $\mathbf{v}_\parallel \in S$ （在子空间内）， $\mathbf{v}_\perp \perp S$ （垂直于子空间）
- 投影矩阵 $P = A(A^TA)^{-1}A^T$ 的作用：接受任意向量 $\mathbf{v}$ ，输出其在 $A$ 的列空间中的投影
- 性质 $P^2 = P$ 的几何含义：**已经投影到子空间中的向量，再投影一次不会改变**（"投影两次等于一次"）

### Gram-Schmidt 的几何步骤

Gram-Schmidt 正交化可视作逐维"减去已有方向的分量"：

1. 取 $\mathbf{v}_1$ 方向作为第一个基方向 $\mathbf{q}_1$
2. 从 $\mathbf{v}_2$ 中减去在 $\mathbf{q}_1$ 上的投影，剩余部分就是 $\mathbf{q}_2$ 
3. 从 $\mathbf{v}_3$ 中减去在 $\mathbf{q}_1$ 和 $\mathbf{q}_2$ 上的投影，剩余部分就是 $\mathbf{q}_3$
4. 依此类推——每一步都是"将当前向量投影到已构造的基上，然后减去"

这等价于：**在每一步消除当前向量与已构造基的相关性**，最终得到一组互不相关的标准正交基。

## Gram-Schmidt 正交化

给定一组线性无关的向量 $\{\mathbf{v}_1, \dots, \mathbf{v}_k\}$ ，可以构造一组标准正交基 $\{\mathbf{q}_1, \dots, \mathbf{q}_k\}$ ：

$$
\mathbf{u}_j = \mathbf{v}_j - \sum_{i=1}^{j-1} \frac{\mathbf{q}_i \cdot \mathbf{v}_j}{\|\mathbf{q}_i\|^2} \mathbf{q}_i, \quad \mathbf{q}_j = \frac{\mathbf{u}_j}{\|\mathbf{u}_j\|}
$$

## 投影

向量 $\mathbf{v}$ 到 $\mathbf{u}$ 方向上的标量投影和向量投影分别为：

$$
\text{proj}_{\mathbf{u}}(\mathbf{v}) = \frac{\mathbf{v} \cdot \mathbf{u}}{\|\mathbf{u}\|^2} \mathbf{u}
$$

更一般地，到由 $A$ 的列空间张成的子空间上的正交投影矩阵为：

$$
P = A(A^TA)^{-1}A^T
$$

性质： $P^2 = P$ ， $P^T = P$ （幂等且对称）。

## ML 应用

| 应用 | 数学形式 | 说明 |
|------|----------|------|
| **残差连接（ResNet）** | $\mathbf{y} = \mathbf{x} + F(\mathbf{x})$ | $\mathbf{x}$ 为输入，$F$ 为残差映射函数，$\mathbf{y}$ 为输出；恒等映射作为投影锚点 |
| **Layer Normalization** | $\hat{\mathbf{x}} = \frac{\mathbf{x} - \mu}{\sigma}$ | $\mathbf{x}$ 为输入，$\mu$ 为均值，$\sigma$ 为标准差，$\hat{\mathbf{x}}$ 为归一化输出；投影到均值为 0 的子空间 |
| **正交初始化** | $W^T W = I$ | $W$ 为权重矩阵；保持梯度信号强度，缓解梯度消失/爆炸 |
| **PCA** | $\mathbf{z} = U^T(\mathbf{x} - \bar{\mathbf{x}})$ | $\mathbf{x}$ 为原始数据，$\bar{\mathbf{x}}$ 为均值向量，$U$ 为主方向矩阵，$\mathbf{z}$ 为投影后的主成分；投影到最大方差方向 |

## 面试追问

**Q1（基础）**：两个向量正交的条件是什么？正交矩阵 $Q$ 满足 $Q^TQ=I$ 有什么关键的数值稳定性优势？
**回答要点**：

1. 正交条件为 $\mathbf{u}\cdot\mathbf{v}=0$，两向量夹角 $90^\circ$，彼此不含对方的信息成分
2. 正交变换保长度 $\|Q\mathbf{x}\|=\|\mathbf{x}\|$，不会放大输入误差，数值稳定性好
3. 正交矩阵求逆即转置 $Q^{-1}=Q^T$，无需数值计算，在数值线性代数中优先使用

**Q2（深挖）**：Gram-Schmidt 正交化过程的核心思想是什么？为什么在实践中更推荐使用 Householder 反射或改进的 Gram-Schmidt？
**回答要点**：

1. 核心是从当前向量中减去已正交化方向的投影分量，逐步构造正交基
2. 经典 GS 数值不稳定，舍入误差会导致正交性损失
3. Householder 反射通过反射变换一次性正交化，稳定性优于 GS；改进的 GS 重排计算顺序也提升稳定性

**Q3（实战）**：在 ResNet 中恒等连接 $\mathbf{y}=\mathbf{x}+F(\mathbf{x})$ 为什么能缓解梯度消失？这跟"投影"有什么概念联系？
**回答要点**：

1. 恒等连接保持了一条梯度直通路径，反向传播时梯度可直接传回浅层不受 $F$ 阻碍
2. 从投影角度看，$\mathbf{x}$ 作为基准投影锚点，$F(\mathbf{x})$ 学习的是偏离基准的残差
3. 当 $F$ 接近零时网络退化为恒等映射，保证了深层至少不差于浅层

**Q4（边界）**：正交投影矩阵 $P=A(A^TA)^{-1}A^T$ 在什么条件下会失效？伪逆投影提供了怎样的替代？
**回答要点**：

1. 当 $A$ 的列不满秩时 $A^TA$ 不可逆，公式失效；需用伪逆 $P=AA^+$ 替代
2. 数值上 $A^TA$ 条件数大时求解不稳定，SVD 方法更可靠
3. $P$ 的秩等于 $\text{rank}(A)$，当秩远小于空间维度时投影信息损失大

## 参考引用
- 需要理解向量基础的相关知识，参见 [向量基础](01-向量基础.md)
- 需要理解向量空间与线性空间的相关知识，参见 [向量空间与线性空间](02-向量空间与线性空间.md)
- 需要理解向量范数的相关知识，参见 [向量范数](03-向量范数.md)
