---
author: "XunZong"
created: "2026-07-06"
tags: ["深度学习", "损失函数", "优化"]
aliases: ["损失函数", "交叉熵", "MSE", "Loss Function"]
---

# 损失函数

## 定义

损失函数（Loss Function）衡量模型预测 $\hat{y}$ 与真实标签 $y$ 之间的差距。训练过程就是最小化损失函数。

## 分类损失

```python
import torch.nn.functional as F

# 二分类交叉熵（输出层用 Sigmoid）
loss = F.binary_cross_entropy(y_pred, y_true)

# 多分类交叉熵（输出层用线性，内部计算 Softmax）
loss = F.cross_entropy(logits, y_true)     # 推荐！数值稳定

# 手动拆开
loss = F.nll_loss(F.log_softmax(logits, dim=1), y_true)
```

| 损失 | 公式 | 适用 | 特点 |
|------|------|:----:|------|
| **交叉熵（Cross-Entropy）** | $-\sum y_i \log \hat{y}_i$ | 分类 | **默认首选**，梯度大，收敛快 |
| **Hinge Loss（合页损失）** | $\sum \max(0, 1 - y_i \hat{y}_i)$ | SVM 分类 | 最大化间隔 |
| **Focal Loss** | $-(1-\hat{y}_t)^\gamma \log \hat{y}_t$ | 类别不平衡 | 降低易分样本权重 |

## 回归损失

```python
# MSE（均方误差）
loss = F.mse_loss(y_pred, y_true)

# MAE（平均绝对误差 / L1 Loss）
loss = F.l1_loss(y_pred, y_true)

# Huber Loss（MSE + MAE 混合）— 详细解析见 [16-光滑损失函数](./16-光滑损失函数.md)
loss = F.huber_loss(y_pred, y_true)
```

| 损失 | 公式 | 对异常值 | 梯度 | 特点 |
|------|------|:--------:|:----:|------|
| **MSE（L2 Loss）** | $\frac{1}{n}\sum(y-\hat{y})^2$ | 敏感 | 随误差增大 | 放大大误差 |
| **MAE（L1 Loss）** | $\frac{1}{n}\sum\vert y-\hat{y}\vert$ | **鲁棒** | 恒定 | 对小误差同等待遇 |
| **Huber Loss** | $\begin{cases} \frac{1}{2}(y-\hat{y})^2 & \vert d\vert<\delta \\ \delta\vert d\vert-\frac{1}{2}\delta^2 & \text{else} \end{cases}$ | 折中 | 自适应 | 综合 MSE 和 MAE 优点 |

## 损失函数选择指南

| 任务 | 输出层激活 | 损失函数 |
|------|-----------|----------|
| 二分类 | Sigmoid | `BCEWithLogitsLoss` |
| 多分类 | Softmax | `CrossEntropyLoss` |
| 多标签分类 | Sigmoid（每个类别独立） | `BCEWithLogitsLoss` |
| 回归 | 恒等（无激活） | `MSELoss` |
| 回归（含异常值） | 恒等 | `HuberLoss` |
| 对比学习 | — | `ContrastiveLoss` / `InfoNCE` |

## ML 中的损失函数

| 应用场景 | 损失函数 | 说明 |
|----------|----------|------|
| 图像分类（ResNet） | CrossEntropyLoss | 标准多分类 |
| 目标检测（YOLO） | 定位（MSE）+ 分类（BCE）+ 置信度 | 多任务联合损失 |
| 图像生成（GAN） | 生成器： $-\log D(G(z))$ ；判别器： $\log D(x) + \log(1-D(G(z)))$ | 对抗损失 |
| 自监督学习 | InfoNCE（对比损失） | SimCLR、MoCo |
| 语言模型（GPT） | CrossEntropyLoss（下一个 token 预测） | 自回归生成 |

## 面试追问

**Q1（基础）**：为什么分类任务通常用交叉熵而不是 MSE？

**回答要点**：交叉熵配合 Softmax 的梯度形式为 (p-y)，与误差成正比，收敛快；MSE 配合 Sigmoid 的梯度含 σ'(z) 项，在饱和区梯度极小导致收敛慢；交叉熵的损失平面更平滑，优化更高效。

**Q2（深挖）**：Focal Loss 如何解决类别不平衡问题？它的设计思路是什么？

**回答要点**：在交叉熵基础上引入调制因子 (1-p_t)^γ；易分样本（p_t 接近 1）的权重被大幅降低，难分样本（p_t 小）的权重相对提升；γ 控制衰减速率，α 平衡正负样本，两者配合解决极度不平衡。

**Q3（实战）**：在多任务学习中同时优化分类和回归损失，你怎么平衡？

**回答要点**：直接加权求和（手动调权重或使用不确定性加权）；确保各损失数值尺度相近，必要时归一化；监控各任务梯度的量级，避免某个任务主导训练；实际项目中常用 grid search 或 GradNorm 自动调节。

**Q4（边界）**：交叉熵在标签噪声大的数据集上有什么问题？如何改进？

**回答要点**：交叉熵会强行拟合错误标签，导致过拟合噪声、泛化下降；改进方案包括标签平滑（Label Smoothing）、对称交叉熵（Symmetric CE）、使用 MAE/L1 损失对噪声更鲁棒；实际中标签平滑最常用，简单有效。

> 参见 [04-反向传播算法](./04-反向传播算法.md)、[06-梯度消失与梯度爆炸](./06-梯度消失与梯度爆炸.md)、[05-权重初始化策略](./05-权重初始化策略.md)
