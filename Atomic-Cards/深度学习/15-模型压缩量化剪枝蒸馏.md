---
author: "XunZong"
created: "2026-07-06"
tags: ["AI-Agent", "模型压缩", "部署"]
aliases: ["模型压缩", "量化", "剪枝", "蒸馏", "模型轻量化"]
---

# 模型压缩（量化 / 剪枝 / 蒸馏）

## 定义

模型压缩将大型模型"瘦身"为适合部署的小模型，在尽量保持精度的前提下减小模型大小、降低推理延迟。三种主流技术分别从**数值精度**（量化）、**结构稀疏性**（剪枝）和**模型架构**（蒸馏）三个维度实现压缩。

## 三大技术对比

| 技术 | 原理 | 压缩比 | 精度损失 | 适用阶段 | 详细卡片 |
|:----:|:----|:------:|:--------:|:--------:|:--------|
| **量化** | 降低参数精度（FP32→INT8） | 4x | 小（<1\%） | 训练后 | [19-模型量化(Quantization)](./19-模型量化(Quantization).md) |
| **剪枝** | 移除不重要连接或通道 | 2-10x | 中 | 训练后/训练中 | [20-模型剪枝(Pruning)](./20-模型剪枝(Pruning).md) |
| **蒸馏** | 小模型模仿大模型输出 | 取决于学生模型 | 小 | 训练时 | [21-知识蒸馏(Distillation)](./21-知识蒸馏(Distillation).md) |

## 组合策略

三种技术可按流水线组合以获得最佳部署效果：

1. **蒸馏** — 先用大模型训练小模型（改变架构）
2. **剪枝** — 移除小模型中的冗余参数（改变结构）
3. **量化** — 降低参数精度（压缩每个参数）

前一步的结果决定后一步的上限，顺序一般建议：先蒸馏，再剪枝，最后量化。

## 面试追问

**Q1（基础）**：量化和剪枝在减小模型尺寸的原理上有什么本质区别？

**回答要点**：量化降低每个参数的存储精度（FP32→INT8 减少 4 倍），不改变网络结构；剪枝直接移除部分参数（置零），改变网络稀疏度；量化对所有参数等比例压缩，剪枝选择性移除"不重要"的参数。

**Q2（深挖）**：为什么非结构化剪枝在通用 GPU 上难以获得实际加速？

**回答要点**：非结构化剪枝产生稀疏权重矩阵，GPU 的并行计算基于密集矩阵乘法优化；稀疏矩阵需特殊硬件或库支持（如 NVIDIA 的稀疏张量核心），通用 GPU 无法利用；结构化剪枝（通道/层级别）权重仍为密集矩阵，可直接获得加速。

**Q3（实战）**：要将 BERT 模型部署到移动端，你会如何组合三种压缩技术？

**回答要点**：先用蒸馏将 BERT-large/base 压缩为 TinyBERT 等小模型；再用训练后动态量化将 FP32 转为 INT8（兼容性好）；若仍需减小体积，用结构化剪枝移除不重要注意力头。顺序是蒸馏→剪枝→量化，前一步决定后一步的上限。

**Q4（边界）**：4-bit 量化对 LLM 的生成质量影响有多大？哪些任务损失最明显？

**回答要点**：4-bit 量化通常保持 90\%+ 的原始质量，但数学推理、代码生成等需要精确数值的任务下降明显；量化误差在低比特下累积，长文本生成中可能产生更多幻觉；不同量化方法（GPTQ vs AWQ vs GGML）效果差异显著，需针对任务评测选择。

> 理解GPU并行与混合精度的工程部署可参见 [GPU并行与混合精度](../工程实践/02-GPU并行与混合精度.md)