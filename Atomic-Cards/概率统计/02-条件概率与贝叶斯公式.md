---
author: "XunZong"
created: "2026-07-06"
tags: ["数学", "概率统计", "贝叶斯"]
aliases: ["条件概率", "贝叶斯公式", "Bayes Theorem"]
---

# 条件概率与贝叶斯公式

## 条件概率

事件 $B$ 发生条件下事件 $A$ 发生的概率：

$$
P(A \vert B) = \frac{P(A \cap B)}{P(B)}, \quad P(B) > 0
$$

**直观理解**：已知 $B$ 已经发生，样本空间从 $\Omega$ 缩小为 $B$ ， $A$ 在缩小后的空间中所占的比例。

## 全概率公式

若 $B_1, B_2, \dots, B_n$ 构成样本空间的一个划分（互斥且全覆盖），则：

$$
P(A) = \sum_{i=1}^n P(A \vert B_i) P(B_i)
$$

## 贝叶斯公式

贝叶斯公式是条件概率的最重要推论，描述了如何根据观测数据更新对某个假设的信念：

$$
P(B_i \vert A) = \frac{P(A \vert B_i) P(B_i)}{P(A)} = \frac{P(A \vert B_i) P(B_i)}{\sum_j P(A \vert B_j) P(B_j)}
$$

| 成分 | 名称 | 含义 |
|------|------|------|
| $P(B_i)$ | **先验概率**（Prior） | 在看到数据前对假设 $B_i$ 的信念 |
| $P(A \vert B_i)$ | **似然**（Likelihood） | 假设 $B_i$ 下观测到数据 $A$ 的概率 |
| $P(B_i \vert A)$ | **后验概率**（Posterior） | 看到数据后对假设 $B_i$ 更新的信念 |
| $P(A)$ | **证据**（Evidence） | 数据出现的总概率（归一化常数） |

## ML 中的关键应用

| 应用 | 贝叶斯形式 | 说明 |
|------|-----------|------|
| **朴素贝叶斯分类器** | $P(y\vert \mathbf{x}) \propto P(y) \prod_i P(x_i\vert y)$ | 条件独立假设下的贝叶斯分类 |
| **贝叶斯推断** | $P(\theta \vert \mathcal{D}) \propto P(\mathcal{D} \vert \theta) P(\theta)$ | 参数的后验分布估计 |
| **最大后验估计（MAP）** | $\hat{\theta}_{\text{MAP}} = \arg\max P(\mathcal{D}\vert\theta)P(\theta)$ | 在 MLE 基础上加入先验 |
| **贝叶斯神经网络** | $P(y\vert x, \mathcal{D}) = \int P(y\vert x, \theta) P(\theta\vert\mathcal{D}) d\theta$ | 对预测不确定性建模 |

> 贝叶斯公式统一了"先验知识"和"观测数据"，是机器学习的概率论基石。交叉熵损失的最小化等价于最大似然估计，L2 正则化等价于高斯先验下的 MAP 估计。

## 面试追问

**Q1（基础）**：贝叶斯公式 $P(B_i|A) = \frac{P(A|B_i)P(B_i)}{P(A)}$ 中四个成分分别叫什么？含义是什么？
**回答要点**：

1. $P(B_i)$ 是先验概率，看到数据前对假设 $B_i$ 的信念
2. $P(A|B_i)$ 是似然，假设 $B_i$ 下观测到 $A$ 的概率
3. $P(B_i|A)$ 是后验概率，看到数据后更新后的信念
4. $P(A)$ 是证据，归一化常数确保后验概率之和为 1

**Q2（深挖）**：全概率公式 $P(A) = \sum P(A|B_i)P(B_i)$ 在贝叶斯推断中扮演什么角色？为什么前向概率的加权总和恰好是归一化常数？
**回答要点**：

1. 全概率公式对"数据 $A$ 在每种假设下出现的概率"按先验加权求和，得到数据出现的总概率
2. 除以 $P(A)$ 保证了 $\sum_i P(B_i|A) = 1$
3. 该求和过程本质是在所有假设空间中对证据进行边际化

**Q3（实战）**：朴素贝叶斯分类器中的"朴素"体现在何处？当条件独立假设被违反时会发生什么？
**回答要点**：

1. "朴素"指假设所有特征在给定类别 $y$ 的条件下相互独立，即 $P(\mathbf{x}|y) = \prod P(x_i|y)$
2. 假设违反时，模型会低估真实特征共同出现的概率，导致过于自信的预测
3. 但实际中即使假设不成立，分类性能通常仍然可接受（尤其特征多时，排序性保持）

**Q4（边界）**：当先验概率和数据高度冲突（如先验认为参数接近 0 但数据强烈指向非零值）时，贝叶斯方法会如何处理？这种冲突在实际中怎样识别？
**回答要点**：

1. 随着数据量增大，先验的影响会被似然"淹没"，后验逐渐收敛到 MLE 解
2. 小数据下冲突严重时，可进行先验敏感性分析（尝试不同先验看后验是否稳定）
3. 若先验与数据系统性不一致，说明先验设定有误——在贝叶斯框架中这本身是一个可诊断的信号

## 参考引用
- 需要理解概率空间与事件运算的相关知识，参见 [概率空间与事件运算](./01-概率空间与事件运算.md)
- 需要理解极大似然估计的相关知识，参见 [极大似然估计](./06-极大似然估计.md)
- 需要理解贝叶斯推断的相关知识，参见 [贝叶斯推断](./07-贝叶斯推断.md)
