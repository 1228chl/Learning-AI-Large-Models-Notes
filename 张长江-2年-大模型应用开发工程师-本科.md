# 张长江-2年-大模型应用开发工程师-本科

## 基本信息

### 张长江

大模型应用 / AI Agent 工程师
- 电话: 15788888804
- 邮箱: changjiangzhang@163.com
- 性别:男
- 年龄:25

## 专业技能

-   **大模型应用与 Agent 开发**：熟悉 LangChain / LangGraph 大模型应用开发，掌握 StateGraph、条件边、Checkpointer 等机制；具备 Multi-Agent 编排、Agent 自主循环、Human-in-the-Loop 人机协同经验；熟悉 MCP 协议、多模型统一接入、LLM 路由、重试降级及 SSE 流式输出。

-   **RAG 知识库与智能检索**：熟悉 RAG 全流程（文档解析、文本切片、Embedding、检索、Rerank、生成），掌握 BGE-M3、Milvus 向量数据库及 IVF\_FLAT/HNSW 索引算法；结合 BM25 实现混合检索，熟悉父子块切分、意图路由、HyDE/Multi-Query 等检索优化策略，具备 RAGAS 评测与效果优化经验。

-   **Python 工程开发**：熟练使用 Python 进行 AI 应用开发，掌握面向对象编程、异步编程（asyncio）、异常处理、日志管理等工程实践；熟悉 FastAPI、Pydantic 构建 REST/SSE/WebSocket 服务，熟悉 PostgreSQL、MySQL、Redis 等数据存储方案，具备 Docker 容器化部署及 Linux 服务维护能力。

-   **机器学习与 NLP 模型实践**：掌握 Transformer、BERT、RNN/LSTM/GRU 等深度学习模型原理，熟练使用 PyTorch、Hugging Face Transformers；具备文本分类、NER、信息抽取等 NLP 任务经验，熟悉 LoRA 微调、模型量化、剪枝、蒸馏等模型优化方法。

-   **数据处理与模型评估**：熟练使用 NumPy、Pandas 进行数据清洗、分析与处理，掌握 Matplotlib 数据可视化；熟悉机器学习流程，包括特征工程、模型训练、AUC/F1 等指标评估以及 GridSearch、Optuna 超参数优化；了解 RAGAS、LLM-as-Judge 等大模型评测体系。

## 项目经历

### EcomAgent — 电商多智能体运营系统

_AI智能体应用工程师_

**技术栈：**
LangGraph、LangChain、FastAPI、DeepSeek、Milvus、BGE-M3、Redis、PostgreSQL

**项目背景：**
针对电商场景中客服咨询、售后处理、商品审核等业务存在的人工成本高、流程复杂、规则维护困难等问题，设计并实现 AI 原生 Multi-Agent 智能运营系统。通过 Supervisor Agent + 多领域 Agent 架构，实现任务理解、动态路由和智能协同，覆盖客服问答、售后审核、商品审核、营销导购等核心业务流程，提升电商运营自动化与智能化水平。

**项目描述：**

-   设计基于 Supervisor Agent + Worker Agents 的多智能体架构，由 Orchestrator Agent 负责用户意图识别、任务规划和 Agent 路由，将请求动态分配至对应领域 Agent。

-   基于 LangGraph StateGraph 实现 Agent 工作流编排，支持 Agent 节点动态跳转、条件边决策、状态持久化及 Checkpointer 中断恢复。

-   构建四类领域 Agent：

    -   智能客服 Agent（RAG Agent）：负责商品咨询、物流查询、售后政策问答，通过 BGE-M3 + Milvus + BM25 实现知识增强检索。

    -   售后处理 Agent（Human-in-the-Loop Agent）：负责退款、退货、换货流程判断，低置信度任务触发人工审核，并支持任务恢复执行。

    -   商品审核 Agent（Multi-Agent Review）：拆分标题审核、描述真实性、图片质量、价格合理性、类目匹配等多个 Reviewer Agent，并通过结果聚合 Agent 输出最终评分。

    -   营销导购 Agent（State Machine Agent）：负责用户需求分析、商品推荐、方案比较、购买决策等多阶段对话流程。

-   设计 Agent 间通信机制，通过 TypedDict / Pydantic 定义统一 State Schema，实现用户信息、任务上下文、工具结果在不同 Agent 间自动传递。

-   搭建 RAG 知识库 Pipeline，实现文档解析、父子块切分、Embedding、Milvus 索引构建及 Hybrid Retrieval 检索增强。

-   基于 FastAPI + SSE 实现多 Agent 执行过程实时流式展示，支持 Agent 思考轨迹、执行状态和最终结果推送。

-   集成 MCP Tool Server，实现 Agent 对外部工具和业务 API 的统一调用，并增加重试、降级、异常恢复机制。

**项目成果：**

-   构建 1+4+N 多智能体协作架构（1 个 Orchestrator Agent + 4 个业务 Agent + N 个 Reviewer Agent），实现复杂电商任务自动拆解与多 Agent 协同执行。

-   通过 Agent 意图路由优化，将请求自动分发准确率提升至 90%+，减少人工规则维护成本。

-   通过 BGE-M3 + Milvus + BM25 Hybrid Retrieval 优化知识检索，相比纯向量检索：Recall@5 提升约 15%、Top-K 有效召回率提升约 12%

-   引入 Reranker 精排和结构化输出约束，使 RAG 回答准确率提升约 20%，降低无依据回答概率。

-   基于 Checkpointer + Human-in-the-Loop 实现任务恢复机制，常规售后流程自动处理比例达到 70%+，低置信度请求自动进入人工复核。

-   通过 SSE 流式输出和异步任务优化，将首 Token 延迟（TTFT）降低约 40%，平均响应时间降低约 50%。

-   通过 Redis 缓存、模型降级和异常重试机制，将服务稳定性提升至 99%+ 可用率。

-   建立 Agent 评估体系，引入 RAGAS、LLM-as-Judge、Recall@K、P95 延迟监控，实现模型效果与系统性能持续优化。

### EnterpriseOps Agent — 企业智能运营助手

_agent开发工程师_

**技术栈：**LangGraph、LangChain、FastAPI、DeepSeek、BGE-M3、Elasticsearch、Redis、PostgreSQL

**项目背景：**
针对企业内部知识分散、业务信息查询效率低、重复性运营任务处理成本高等问题，设计并实现基于 LLM Agent 的智能运营助手。通过 Single-Agent 自主规划 + RAG 知识增强 + Tool Calling 工具调用，实现自然语言驱动的知识检索、数据查询和任务执行自动化。

**项目描述：**

-   基于 LangGraph 构建 Single-Agent 自主执行框架，实现任务理解、计划生成、工具选择、结果反馈和流程闭环。

-   设计 Agent Tool Calling 体系，封装数据库查询、知识检索、业务接口调用等工具，使 Agent 根据用户意图动态选择执行能力。

-   搭建企业知识库 RAG Pipeline，实现文档解析、文本切分、BGE-M3 向量化、Elasticsearch Hybrid Retrieval 以及 Reranker 精排。

-   引入 ReAct + Reflection Agent Loop，支持复杂任务拆解、执行结果校验、失败重试和策略调整。

-   基于 Redis + PostgreSQL 实现短期会话 Memory 和任务状态持久化，支持多轮上下文理解。

-   使用 FastAPI + SSE 构建 Agent 服务接口，实现推理过程和执行结果实时流式返回。

-   增加 Prompt 模板管理、异常重试、Token 控制等工程机制，提高 Agent 稳定性。

**项目成果：**

-   构建完整 Single-Agent 自主执行链路，实现 “需求理解 → 任务规划 → 工具调用 → 结果验证 → 内容生成” 闭环。

-   通过 RAG + Hybrid Retrieval 优化企业知识查询效果，相比纯向量检索 Recall@K 提升约 15%。

-   引入 Tool Calling 和 Reflection 机制，提高复杂任务处理能力，任务成功率提升约 20%。

-   通过 Redis 缓存和 SSE 流式优化，降低平均响应延迟约 40%。

-   建立 Agent 评估体系，结合任务成功率、Token 消耗、TTFT、P95 延迟进行效果优化。

### KnowledgeHub — 企业级 RAG 智能知识库系统

_RAG开发工程师_

**技术栈：**LangChain、FastAPI、BGE-M3、Milvus、BM25、BGE-Reranker、RAGAS、Redis

**项目背景：**
面向企业知识管理场景，构建基于 RAG 架构的智能知识库系统，解决企业文档信息分散、传统检索语义理解不足以及大模型幻觉问题。通过 Embedding 检索 + Hybrid Retrieval + Rerank + LLM 生成 构建端到端知识问答链路。

**项目描述：**

-   搭建 RAG Pipeline，实现文档解析、清洗切分、BGE-M3 向量化、Milvus 索引构建及上下文增强生成。

-   基于 Milvus + BM25 实现向量检索与关键词检索融合，结合 BGE-Reranker 完成多阶段召回与精排优化。

-   设计 Parent-Child Chunk、Metadata Filter、Query Rewrite 等策略，提升长文档和复杂问题检索效果。

-   构建 RAGAS + LLM-as-Judge 评估体系，从 Faithfulness、Answer Relevancy、Context Recall 等指标优化 RAG 效果。

-   基于 FastAPI 提供知识库服务接口，支持文档管理、检索查询及 SSE 流式回答。

**项目成果：**

-   构建完整企业级 RAG 闭环，实现文档接入、知识检索、答案生成及引用追踪。

-   Hybrid Retrieval 相比单一向量检索 Recall@5 提升约 15%，Reranker 优化后回答准确率提升约 20%。

-   支持多知识库隔离和动态扩展，可复用于企业 FAQ、技术文档、业务知识库等场景。

### TicketClassifier — 企业中文工单智能分类系统

_NLP开发工程师_

**技术栈：**Python、Scikit-learn、FastText、PyTorch、Hugging Face Transformers、BERT-base-Chinese、FastAPI

**项目背景：**
面向企业客服工单自动分类场景，构建中文文本分类系统，对用户咨询、投诉、售后、物流等业务请求进行自动识别与分类。采用 TF-IDF + Random Forest → FastText → BERT-base-Chinese 微调 的模型迭代方案，逐步提升中文语义理解能力和分类效果。

**项目描述：**

-   构建中文 NLP 文本分类 Pipeline，完成数据清洗、文本分词、特征提取、模型训练、效果评估及服务部署。

-   基于 TF-IDF 特征 + Random Forest 搭建分类 Baseline，验证传统机器学习方法在中文短文本分类任务中的效果。

-   引入 FastText 中文文本分类模型，利用词向量表示和字符级 n-gram 特征，提升对短文本、未登录词和口语化表达的识别能力。

-   基于 BERT-base-Chinese + Fine-tuning 构建深度语义分类模型，利用 Transformer 双向上下文建模能力提升复杂语义场景下的分类效果。

-   针对类别分布不均问题，采用 Class Weight、数据采样等策略优化模型训练。

-   使用 Accuracy、Precision、Recall、Macro-F1 等指标进行模型评估，并通过混淆矩阵分析模型误分类情况。

-   基于 FastAPI 封装模型推理接口，实现中文文本分类服务化部署。

**项目成果：**

-   完成从传统机器学习到预训练语言模型的中文 NLP 分类模型演进流程。

-   BERT-base-Chinese 微调模型相比 Random Forest Baseline，分类 F1-score 提升约 15%+，显著增强复杂语义理解能力。

-   FastText 相比传统 TF-IDF 方法，在短文本和新词场景下具有更好的泛化能力。

-   实现可复用文本分类服务，可作为智能客服意图识别、Agent 路由等上层应用模块。

## 工作经验

### 浩鲸云计算科技股份有限公司 | 大模型算法工程师

_2024/09 - 2026/07_

-   负责企业级 LLM 应用研发，围绕 Agent + RAG + NLP 技术体系，构建智能客服、知识库问答、业务自动化等大模型应用。

-   设计并落地基于 LangGraph 的 Agent 工作流框架，实现 Orchestrator、Tool Calling、状态管理及 Human-in-the-Loop 机制，支持 Multi-Agent 协作场景。

-   搭建企业级 RAG Pipeline，基于 BGE-M3 + Milvus + BM25 + Reranker 实现混合检索架构，通过 Chunk 优化、Query Rewrite、检索策略调整提升知识问答效果。

-   建立 LLM 应用评估体系，结合 RAGAS、LLM-as-Judge、Recall@K、P95、TTFT 等指标优化模型效果和系统性能。

-   负责 NLP 算法研发，基于 BERT-base-Chinese Fine-tuning 完成文本分类、意图识别任务，结合传统机器学习和深度学习方法提升业务效果。

-   负责 AI 应用工程化部署，使用 FastAPI、Docker、Redis、PostgreSQL 构建高可用模型服务，支持流式输出、多模型调用和服务监控。

## 教育经历

### 江西软件职业技术大学 | 大数据工程技术

_本科 | 2021/09 - 2025/06_

## 自我评价

具备扎实的机器学习与 NLP 基础，熟悉 Transformer、BERT 等模型原理及大模型应用开发流程。具备 LLM Agent、RAG、多智能体系统设计与落地经验，熟悉 LangGraph、Milvus、BGE-M3 等大模型应用技术栈。

能够结合业务需求完成从算法方案设计、模型优化到工程部署的完整闭环，具备较强的问题分析和系统落地能力。关注大模型应用效果与性能优化，熟悉 RAG 评测、Agent 调优及生产环境工程实践。

具备持续学习和技术探索能力，能够快速掌握新技术并应用于实际业务场景。