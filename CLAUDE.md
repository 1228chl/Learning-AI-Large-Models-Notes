# AI-Learning 项目成本控制与 API 质量规则

## 核心原则

**每次 API 请求都有成本。在调用前必须确保：问题完整、方案明确、操作精准。**

---

## 规则一：质量门禁（Quality Gate）

以下操作**必须先整理思路再执行**，禁止直接发起 API 请求：

| 场景 | 必须做的 | 禁止做的 |
|:-----|:---------|:---------|
| **读文件** | 先用 glob/grep 定位，明确要读什么 | 连续多次 Read 探索文件 |
| **写卡片** | 一次性 Write 完整卡片 | 写一半→读→改→再读→再改 |
| **批量操作** | 先写完整 Python 脚本再执行 | 每步单独调 agent 或 shell |
| **搜索内容** | 先用精确的 grep 或 glob 定位 | 大范围 Read 后手动找 |
| **修改代码** | 先 Read 确认完整内容再 Edit | 小范围 Edit 反复试错 |

## 规则二：请求合并（Request Batching）

- **关联操作一次完成**：需要读 3 个文件时，同时读（并行 Read 调用），不要读完一个再读下一个
- **批量处理用脚本**：任何 >5 个文件的机械操作 → Python 脚本，0 token 消耗
- **多步操作规划后再执行**：复杂操作（如重构目录结构）先陈述方案，确认后再执行

## 规则三：输出精简

- **输出是真正的成本**（$0.28/M tokens），输出文字能省则省
- 确认类操作：直接汇报结果，不需要"请问是否继续"（除非不可逆）
- 代码展示：优先用 diff 或关键行注释，不展示完整文件
- 回答格式：不需要完整展示时只给结论和关键数据

## 规则四：会话生命周期

- 单会话 ≤ 50 轮或 ≤ 1 天
- 任务完成立即新开会话
- 简单查询/指令也新开（避免小请求被大上下文拖累）
- 跨天会话必须新开（缓存已过期，继续只有成本没有收益）

## 规则五：cc-switch 代理控制

- 代理已启用（`proxy_enabled: 1`），自动熔断高频错误请求
- `max_retries: 2` - 最多重试 2 次，失败快速降级
- 非流式超时 600 秒 - 长时间请求自动终止